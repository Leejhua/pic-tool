version: '3.8'

services:
  pic-tool:
    build: .
    image: pic-tool:latest
    container_name: pic-tool-container
    ports:
      - "8080:80"
    restart: unless-stopped
    # 移除自定义网络，改用默认 bridge 网络，避免 IP 池耗尽或冲突问题
    network_mode: bridge
